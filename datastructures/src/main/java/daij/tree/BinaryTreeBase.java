package daij.tree;

/**
 * BinaryTree base class. A binary tree is a tree but each node has no more than two child.
 *
 * @param <E> Element type of the Node
 */
public abstract class BinaryTreeBase<E> implements Tree<E>, Binary<E> {

}
